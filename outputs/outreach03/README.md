# Outreach 03 — three evidence requests

This package completes the administrative preparation requested by GitHub Issue #8.
It does not change the scientific record or the manuscript.

## Disposition

- Palmer/Ward: the project state records the request as **SENT / PENDING**, but
  the exact provider receipt is not present in the bridge. The package therefore
  records `BLOCKED_USER_ACTION` and **USER-REPORTED SENT / PENDING —
  UNVERIFIED**, and does not resend the message.
- Watson/Chow: the project state records the request as **SENT / PENDING**, but
  the exact provider receipt is not present in the bridge. The package therefore
  records `BLOCKED_USER_ACTION` and **USER-REPORTED SENT / PENDING —
  UNVERIFIED**, and does not resend the message.
- ADEC: **BLOCKED_USER_ACTION**. The form and request text are ready, but the
  requester must supply identity/contact fields, truthfully address the
  nonlitigation statement, and explicitly submit. A signature is needed only if
  the optional DOCX/hard-copy form is used. Codex did not submit it.

## Read order

1. `OUTREACH_STATUS.csv`
2. `KEEP_HOLD_REMOVE.md`
3. the three request/follow-up sets
4. `SUBMIT_THIS_NOW.md` (ADEC user-action handoff; do not send from Codex)
5. `CONTACTS_VERIFIED.csv` and `source_inventory.csv`
6. `OUTREACH_LOG.md`
7. `reproduction.md`

## Locked boundary

The active baseline remains **PFFLS R4.3.30R3nR7-AE**. No manuscript file,
locked result, claim, source selector, or validation decision is changed.
